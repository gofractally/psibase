package {{project-name}}:plugin;

interface admin {
    use host:common/types.{error};
    save-perm: func(caller: string, method: string) -> result<_, error>;
    del-perm: func(caller: string, method: string) -> result<_, error>;
}

world impl {
    include host:common/imports;
    include transact:plugin/imports;
    include sites:plugin/imports;
    include clientdata:plugin/imports;
    include accounts:plugin/imports;

    export admin;
    export api;
    export queries;
}
