{
    // This file is used by shared components and assets at the /services directory level and by the shadcn CLI
    "extends": "./tsconfig.app.json",
    "compilerOptions": {
        "baseUrl": ".",
        "paths": {
            "@shared/*": ["shared-ui/*"],
            "@psibase/common-lib": [
                "./user/CommonApi/common/packages/common-lib/src"
            ]
        }
    }
}
