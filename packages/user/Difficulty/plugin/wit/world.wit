package difficulty:plugin;

interface api {
    use host:types/types.{error};
    create: func(token-id: u32, floor-price: string, window-seconds: u32, percent-change: u8, target: u32) -> result<_, error>;
    increment: func(token-id: u32) -> result<_, error>;
    delete: func(token-id: u32) -> result<_, error>;
}


world imports {
    import api;
}
