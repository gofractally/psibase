package fractal-core:plugin;

interface api {
    use host:types/types.{error};
    start-eval: func(guild-id: u32) -> result<_, error>;

    join: func() -> result<_, error>;
}


world imports {
    import api;
}
