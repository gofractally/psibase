[package]
name = "frac-tester-pkg"
description = "Adds 18 auth-any test accounts (testfrac01 through testfrac18) to genesis guild of the system fractal. Not for production use."
version.workspace = true
edition.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
publish.workspace = true

[package.metadata.psibase]
package-name = "FractalTester"
services = ["frac-tester"]

[lib]
crate-type = ["rlib"]

[dependencies]
frac-tester = { path = "service", version = "0.24.0" }
psibase-user-workspace-hack.workspace = true
