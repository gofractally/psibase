package host:common;

world impl {
    include platform:bridge/imports;
    import accounts:plugin/api;
    include host:auth/imports;

    export server;
    export client;
    export admin;
    export store;
}
