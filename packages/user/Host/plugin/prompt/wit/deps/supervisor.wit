package supervisor:bridge;

interface prompt {
    /// Requests that the host show the active prompt
    /// Can return an error string
    request-prompt: func() -> result<_, string>;
}

world imports {
    import prompt;
}
