package supervisor:bridge;

interface prompt {
    use host:types/types.{error};

    /// Requests that the host show the active prompt
    request-prompt: func() -> result<_, error>;
}

world imports {
    import prompt;
}
