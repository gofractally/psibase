[package]
name = "identity-pkg"                                         # Crate name
description = "Tools for exploring and confirming identities"
version.workspace = true
rust-version.workspace = true
edition.workspace = true
repository.workspace = true
homepage.workspace = true
publish.workspace = true

[package.metadata.psibase]
package-name = "Identity"
services = ["identity"]

[package.metadata.psibase.dependencies]
Accounts = "0.24.0"
Host = "0.24.0"
HttpServer = "0.24.0"
Sites = "0.24.0"
Transact = "0.24.0"
Permissions = "0.24.0"

[lib]
crate-type = ["rlib"]

[dependencies]
identity = { path = "service", version = "0.24.0" }
identity-plugin = { path = "plugin", version = "0.24.0" }
psibase-user-workspace-hack.workspace = true
