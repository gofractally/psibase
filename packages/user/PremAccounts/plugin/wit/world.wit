package prem-accounts:plugin;

interface api {
    use host:types/types.{error};
    buy: func(account: string, max-cost: u64) -> result<_, error>;
    claim: func(account: string, pub-key: string) -> result<_, error>;
}

interface queries {
    use host:types/types.{error};
    get-prices: func() -> result<list<u64>, error>;
}

world imports {
    import api;
    import queries;
}
