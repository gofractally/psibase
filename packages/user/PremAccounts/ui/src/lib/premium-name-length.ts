/**
 * Premium account name length bounds for the PremAccounts app UI.
 * Keep in sync with `prem_accounts::constants` in the PremAccounts service crate.
 */
export const MIN_PREMIUM_NAME_LENGTH = 1;
export const MAX_PREMIUM_NAME_LENGTH = 15;
