[package]
name = "profiles-pkg"
description = "Add profiles"
version.workspace = true
rust-version.workspace = true
edition.workspace = true
repository.workspace = true
homepage.workspace = true
publish.workspace = true

[package.metadata.psibase]
package-name = "Profiles"
services = ["profiles"]

[package.metadata.psibase.dependencies]
HttpServer = "0.22.0"
Sites = "0.22.0"

[lib]
crate-type = ["rlib"]

[dependencies]
profiles = { path = "service", version = "0.22.0" }
r-profiles = { path = "query-service", version = "0.22.0" }
profiles-plugin = { path = "plugin", version = "0.22.0" }
user-workspace-hack = { version = "0.1", path = "../user-workspace-hack" }
