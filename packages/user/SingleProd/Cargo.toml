[package]
name = "single-prod-pkg"
description = "One-time setup for a single-provider private network. Install at boot instead of FractalGen."
version.workspace = true
edition.workspace = true
repository.workspace = true
homepage.workspace = true
rust-version.workspace = true
publish.workspace = true

[package.metadata.psibase]
package-name = "SingleProd"
services = ["single-prod"]

[lib]
crate-type = ["rlib"]

[dependencies]
single-prod = { path = "service", version = "0.29.0" }
psibase-user-workspace-hack.workspace = true
