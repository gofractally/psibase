package symbol:plugin;

interface api {
    use host:types/types.{error};
    purchase: func(symbol: string) -> result<_, error>;

    start-sale: func(len: u8, token-id: u32, initial-price: string, window-seconds: u32, target-min: u32, target-max: u32, floor-price: string, percent-change: u32) -> result<_, error>;
}


world imports {
    import api;
}
