{
    // This file is referenced and used by child UI projects for the UI
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "target": "ES2022",
        "useDefineForClassFields": true,
        "lib": ["ES2022", "DOM", "DOM.Iterable"],

        /* Bundler mode */
        "jsx": "react-jsx"
    }
}
