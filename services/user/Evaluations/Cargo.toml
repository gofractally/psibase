[workspace]
resolver = "2"
members = ["service", "plugin"]

[workspace.package]
version = "0.15.0"
rust-version = "1.64"
edition = "2021"
publish = false

[package]
name = "evaluations-pkg"   # Crate name
description = "Evaluate in random groups"
version.workspace = true
rust-version.workspace = true
edition.workspace = true
publish.workspace = true

[profile.release]
codegen-units = 1
opt-level = "s"
debug = false
strip = true
lto = true

[package.metadata.psibase]
package-name = "Evaluations"
services = ["evaluations"]

[package.metadata.psibase.dependencies]
HttpServer = "0.15.0"
Sites = "0.15.0"

[lib]
crate-type = ["rlib"]

[dependencies]
evaluations = { path = "service", version = "0.15.0" }
r-evaluations = { path = "query-service", version = "0.15.0" }
plugin = { path = "plugin", version = "0.15.0" }
