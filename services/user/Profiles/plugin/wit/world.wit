package profiles:plugin;

interface types {
 
    record profile {
        display-name: option<string>,
        bio: option<string>,
    }

}

interface api {
    
    use types.{profile};

    set-profile: func(profile: profile);
}


world imports {
    import api;
}
