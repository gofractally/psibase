import { Layout } from "components";
import { Wallet } from "./views";

export const Home = () => {
    return (
        <Layout>
            <Wallet />
        </Layout>
    );
};

export default Home;
